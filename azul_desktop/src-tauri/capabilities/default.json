{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allows the main desktop window to use core IPC commands and the native dialog open action.",
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
