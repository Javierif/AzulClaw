# Layouts

Use this folder for higher-order layout shells when a feature grows beyond a single screen.

At the moment, most composition happens directly in `src/app/` and feature shells, so this folder remains intentionally light.
