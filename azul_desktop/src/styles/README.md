# Styles

Global theme assets live here.

- `global.css` defines the desktop look and feel.
- Use this folder for design tokens, shared variables, and layout-wide styling rules.
- Feature-specific overrides should stay close to the feature unless reused broadly.
