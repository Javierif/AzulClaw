# Components

Shared UI primitives used across multiple desktop views live here.

Current examples include:

- `Sidebar.tsx` for primary navigation
- `Toggle.tsx` for compact state switching
- `Tooltip.tsx` for hover affordances

Keep this folder focused on reusable interface pieces, not feature-specific logic.
