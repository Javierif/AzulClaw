# Processes

This feature shows recent and active runtime work.

- `ProcessesShell.tsx` renders process summaries from `/api/desktop/processes`.
- The view is useful for long-running turns, scheduled jobs, and operational visibility.
- It complements chat commentary rather than replacing it.
